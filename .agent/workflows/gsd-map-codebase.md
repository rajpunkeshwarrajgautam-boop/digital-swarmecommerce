---
description: Analyze the current project following GSD v1 protocol.
---

1. **Audit Files**: Scan the root and `src` for technology stack, naming conventions, and design patterns.
2. **Context Engineering**: Identify key files for the design system (e.g., `VisualQuality.tsx`, `globals.css`) and architecture (e.g., `package.json`, `next.config.js`).
3. **Initialize Specs**: Create or update `PROJECT.md` and `REQUIREMENTS.md` with the current project status.
4. **Roadmap Generation**: Suggest 3 milestones based on the analysis.
5. **Verify**: Check for console errors or broken dependencies.
