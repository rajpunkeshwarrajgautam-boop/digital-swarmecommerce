# Phase 4.1: Multi-currency support - Plan

This plan implements multi-currency support for Digital Swarm, focusing on dynamic pricing and transactional consistency.

## Tasks

### 1. Currency Utilities [src/lib/utils.ts]
- [ ] Implement `getExchangeRate(target: string)` with static table (INR, USD, EUR, GBP).
- [ ] Enhance `formatPrice(amount: number, currency: string)` to use `Intl.NumberFormat`.

### 2. UI Components
- [ ] Create `src/components/CurrencySwitcher.tsx` using Planet ONO industrial aesthetic (HSL colors, sharp borders).
- [ ] Integrate `CurrencySwitcher` into the Header (`src/app/layout.tsx` or `src/components/Header.tsx`).

### 3. Product Pricing Display [src/app/product/[slug]/AddToCartButton.tsx]
- [ ] Update to use the current currency selection.
- [ ] Ensure prices are computed on the fly from the basePrice (INR).

### 4. Transactional Logic [src/app/api/cashfree/create-order/route.ts]
- [ ] Update Cashfree API call to pass the `order_currency`.
- [ ] Map the platform's currency selection to Cashfree's accepted codes.

### 5. Verification
- [ ] Verify build passes.
- [ ] Check currency switching in different UI components.
- [ ] Log test order payload for correct currency.

## Verification Checklist

- [ ] All prices show selected currency symbol.
- [ ] Amounts are correctly converted.
- [ ] Cashfree payload includes chosen currency.
- [ ] Selection persists after page refresh.
